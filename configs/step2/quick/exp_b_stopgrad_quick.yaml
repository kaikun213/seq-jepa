# Experiment B: Stop-Gradient (Teacherless) + Coding-Rate (Quick Test)
# Purpose: Verify learning without EMA, check for collapse

run:
  name: step2-exp-b-stopgrad-quick
  seed: 42
  device: auto
  mps_fallback: true
  epochs: 5
  output_dir: runs/step2/exp-b-stopgrad-quick
  drop_last: true

dataset:
  name: cifar10_rot
  data_root: data
  seq_len: 3
  rotations: [0, 90, 180, 270]
  download: true
  subset_train: 5000
  subset_val: 1000
  subset_seed: 42
  batch_size: 64
  num_workers: 0

model:
  img_size: 32
  n_channels: 3
  ema: false
  ema_decay: 0.996
  teacherless: true
  pred_hidden: 1024
  num_heads: 4
  num_enc_layers: 3
  act_cond: 1
  learn_act_emb: 1
  act_latentdim: 2
  act_projdim: 128
  cifar_resnet: true
  sharpening_enabled: false
  symmetric: false

loss:
  rate_loss_enabled: true
  lambda_rate: 0.01
  alpha: 1.0
  rate_target: agg_out
  rate_use_stable: false

optim:
  lr: 0.001
  weight_decay: 0.0
  probe_lr: 0.001

eval:
  latent_type: rot
  eval_type: rot
  subspace_enabled: true
  subspace_frequency: 5
  subspace_viz: false

logging:
  wandb:
    enabled: true
    project: seq-jepa-streaming
    group: step2-exp-b-quick
    mode: online

gating:
  linacc_test_min: 20.0
  r2_test_min: 0.0
  leakage_linacc_gap_min: 0.0
  leakage_r2_gap_min: 0.0
  gap_action_min: -0.1
  # Collapse detection
  rank_eq_eff_min: 5
  rank_inv_eff_min: 5

